function NotFoundPage () {
    return (
        <>
            This is the page to display if the wrong address is typed into the address bar

            Error 404: the page you're looking for could not be found
        </>
    )
}

export default NotFoundPage;
